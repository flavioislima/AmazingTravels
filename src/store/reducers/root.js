const initialState = {
    places: [],
    selectedPlaces: null
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        case value: 

            break;
    
        default:
            return state;
    }
}

export default reducer